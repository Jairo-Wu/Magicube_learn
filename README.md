# Magicube_learn
魔方 CFOP 公式学习网站。

## 访问

GitHub Pages 发布后可通过项目页面访问。这个仓库保留了原来的 PHP 文件，同时新增 `index.html` 作为静态发布入口，适合直接部署到 GitHub Pages。

## 文件说明

- `index.html`：由数据库备份中的公式数据生成的静态首页，用于 GitHub Pages。
- `index.php`：原 PHP 动态首页备份，需要 PHP + MySQL 环境。
- `cube/`、`upload/`、`css/`、`js/`、`img/`：网页运行所需的静态资源。
